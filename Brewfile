# The window needs Tk 9. Tk 8, which macOS and most other python3 builds carry,
# hands macOS the click that raises a window and never gets it back, leaving every
# button dead until the app is picked off the menu bar.
brew "python-tk@3.13"
